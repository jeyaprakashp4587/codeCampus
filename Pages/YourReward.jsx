import { StyleSheet, Text, View } from "react-native";
import React from "react";

const YourReward = () => {
  return (
    <View>
      <Text>YourReward</Text>
    </View>
  );
};

export default React.memo(YourReward);

const styles = StyleSheet.create({});
